# schema=router-egress-provider-direct-config-v1
# R20Q-U-B1: installed but disabled-until-proven; eth0 HMN transport has no production proof.
PROVIDER_DIRECT_ENABLED=0
PROVIDER_DIRECT_TICK_SEC=30
PROVIDER_DIRECT_RETRY_INTERVAL_SEC=600
PROVIDER_DIRECT_READY_REUSE_SEC=900
PROVIDER_DIRECT_STATE_DIR=/etc/router-egress-provider-direct
PROVIDER_DIRECT_STATE_FILE=/etc/router-egress-provider-direct/state.kv
PROVIDER_DIRECT_LOCK_DIR=/var/run/router-egress-provider-direct.lock
PROVIDER_DIRECT_DIRECT_STATE=/etc/router-wgpay-direct/state.kv
PROVIDER_DIRECT_STATUS_CMD=/usr/local/sbin/router-egress-slots-status.sh
PROVIDER_DIRECT_DOWNLOAD_CMD=/root/hmn/hmn-download-all-awg.sh
PROVIDER_DIRECT_RECOVERY_STATE_HELPER=/usr/local/lib/router-egress-recovery-state.sh
PROVIDER_DIRECT_INTERFACE=eth0
PROVIDER_DIRECT_NETWORK_CONFIG=/etc/config/network
PROVIDER_DIRECT_PROVIDER_ROOT=/root/hmn
PROVIDER_DIRECT_LOG=/var/log/router-egress-provider-direct.log
