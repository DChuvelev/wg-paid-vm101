# schema=router-egress-recovery-hmn-v2
# STEP_050M07R15A_LEGACY_EXECUTION_FREEZE_AND_CLEAN_FOUNDATION

VM101_CONF=/etc/router-egress-vm101.conf
[ -r "$VM101_CONF" ] && . "$VM101_CONF"

MODE=--dry-run
SLOTS_CONF=${SLOTS_CONF:-/etc/router-egress-slots.d/slots.conf}
HMN_CACHE_DIR=${PROVIDER_ROOT:-/root/hmn}/cache
MAX_POOL_AGE_SEC=129600
PREFERRED_POOL_FILES="ok-awg1-strict-foreign-latest.tsv ok-awg1-strict-all-latest.tsv working-awg1-latest.tsv ranked-awg1-latest.tsv"
STATE_DIR=${STATE_DIR:-/var/lib/router-egress-recovery}
STATE_HELPER=${RECOVERY_STATE_HELPER:-/usr/local/lib/router-egress-recovery-state.sh}
LOG=${RECOVERY_LOG:-/var/log/router-egress-recovery-hmn.log}
REQUIRE_EXPLICIT_COMMIT=1
POST_APPLY_SLEEP_SEC=12
LOCAL_REPAIR_CANDIDATE_RETRIES=${LOCAL_REPAIR_CANDIDATE_RETRIES:-3}
FULL_REFRESH_AFTER_REPAIRS=${FULL_REFRESH_AFTER_REPAIRS:-5}
